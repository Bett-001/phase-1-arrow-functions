// 1️Function expression - divide two numbers
const divide = function(num1, num2) {
  return num1 / num2;
};

// 2️Arrow function - square a number
const square = x => x * x;

// 3️Arrow function - add two numbers
const add = (a, b) => a + b;
